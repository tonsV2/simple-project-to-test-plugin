buildscript {
    ext {
        kotlinVersion = '1.3.71'
    }
    repositories {
        mavenCentral()
        mavenLocal()
    }
    dependencies {
        classpath 'dk.fitfit.gradle:source-release:1.0-SNAPSHOT'
    }
}

apply plugin: 'source-release'

group = 'dk.fitfit.simple'
version = '0.30.0-SNAPSHOT'

task test {
    println("Test task!!!")
}
